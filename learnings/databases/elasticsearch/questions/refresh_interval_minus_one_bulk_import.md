---
tags: [elasticsearch, refresh_interval, bulk-import, indexing]
status: In Progress
related_topics: []
---

# Refresh interval -1 during bulk import then set back

## ❓ The Core Question

> "We're doing a big one-time bulk import, so we set `refresh_interval` to `-1` and will set it back to `1s` after the import. That way we avoid any refresh overhead during the import."
>
> What important thing are we forgetting when we do this?

## 🧠 The Learning Log

*(Refinements, misconceptions, and follow-ups will be added here as we go.)*
